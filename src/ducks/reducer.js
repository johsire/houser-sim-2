const initalState = {
  name: '',
  address: '',
  city: '',
  state: '',
  zip: 0,
}

function reducer(state = initalState, action) {
  switch(action.type) {
    default: return state; 
  }

}

export default reducer;